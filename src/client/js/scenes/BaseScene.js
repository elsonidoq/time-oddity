import Phaser from 'phaser';

export default class BaseScene extends Phaser.Scene {
  constructor(key) {
    super(key);
  }

  init(data) {
    // Standard scene init method
  }

  preload() {
    // Standard scene preload method
  }

  create(data) {
    // Standard scene create method
  }

  update(time, delta) {
    // Standard scene update method
  }
} 